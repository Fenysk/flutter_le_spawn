import 'package:flutter/material.dart';

class VideoGameItem extends StatelessWidget {
  const VideoGameItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(
      child: SizedBox(
        height: 200,
        child: Placeholder(),
      ),
    );
  }
}
